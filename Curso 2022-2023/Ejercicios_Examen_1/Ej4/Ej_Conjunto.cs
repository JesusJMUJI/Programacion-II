using System;
using System.Collections.Generic;
using System.Linq;
using System.Security.Cryptography;
using System.Text;
using System.Threading.Tasks;

namespace Ej4
{
    /*
    4. Definir una clase Conjunto que contenga un array de valores de cualquier tipo y un método Intersección que recibe un Conjunto como parámetro y devuelva un nuevo Conjunto con la intersección de los dos, los elementos del Conjunto recibido que son iguales a algunos elementos contenidos en el array de valores del objeto Conjunto.Se puede comprobar que dos objetos son iguales mediante el método estático Equals de la clase Object.
    */
    
    public class Conjunto
    {

        private int[] Array { get; set; }

        public Conjunto Intersección(Conjunto conjunto)
        {
            foreach (var item in Array) 
            { 
                
            }
        }
    }
}
